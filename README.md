# Интерактивный анализатор рынка подержанных автомобилей

Этот проект представляет собой комплексный инструмент для анализа рынка подержанных автомобилей с нескольких сайтов, включая `polovniautomobili.com` и `mobile.de`.

Он состоит из двух основных частей:
1.  **Скрипты для сбора данных:** Гибкие скраперы для каждого сайта (`src/scrape_polovni_botasaurus.py`, `src/scrape_mobile_de.py`), которые собирают объявления по заданным поисковым запросам.
2.  **Интерактивное веб-приложение (`app.py`):** Приложение на базе Streamlit для визуализации, фильтрации и сравнительного анализа собранных данных.

---

## 🚀 Основные возможности приложения

*   **Сравнение рынков:** Возможность загружать и сравнивать данные по одной и той же модели с разных сайтов.
*   **Сравнительный анализ:** Одновременное отображение нескольких групп автомобилей (например, BMW X5 с `polovniautomobili.com` и BMW X5 с `mobile.de`) на одном графике.
*   **Детальное сравнение цен:** Подробный статистический анализ и визуализация распределения цен для выбранной модели по разным источникам.
*   **Линии тренда:** Автоматическое построение отдельных линий регрессии для каждой группы, показывающих зависимость цены от пробега и года.
*   **Интерактивность:**
    *   Кликабельные точки на графике, ведущие на страницу объявления (функция работает как в приложении, так и в HTML-отчетах).
    *   Гибкие фильтры по источнику данных, группам, году выпуска и пробегу.
*   **Поиск выгодных предложений:** Умная таблица, которая показывает Топ-2 самых дешевых автомобиля для каждой группы в рамках выбранной категории пробега (0-50 тыс. км, 50-100 тыс. км и т.д.).
*   **Экспорт отчетов:** Возможность сохранить текущий вид анализа (график + таблица) в единый, интерактивный и сортируемый HTML-файл.

---

## 🛠️ Рабочий процесс

### Шаг 1: Сбор и обновление данных

Вы можете собирать данные с любого из поддерживаемых сайтов, запуская соответствующий скрипт.

#### Сбор данных с `polovniautomobili.com`

1.  **Настройка:** Откройте файл `src/scrape_polovni_botasaurus.py`. В самом низу файла найдите и отредактируйте словарь `SEARCH_QUERIES`, добавив или изменив нужные вам ссылки для поиска.
2.  **Запуск:** Выполните в терминале команду:
    ```bash
    python3 src/scrape_polovni_botasaurus.py
    ```

#### Сбор данных с `mobile.de`

1.  **Настройка:** Откройте файл `src/scrape_mobile_de.py`. В самом низу файла найдите и измените переменную `SEARCH_URL` на нужную вам ссылку для поиска.
2.  **Запуск:** Выполните в терминале команду:
    ```bash
    python3 src/scrape_mobile_de.py
    ```

### Шаг 2: Запуск интерактивного приложения

Для анализа и визуализации данных запустите приложение:

```bash
streamlit run app.py
```
В вашем браузере откроется вкладка с приложением. В боковой панели вы сможете выбрать, данные с каких сайтов и по каким моделям вы хотите видеть.

### Шаг 3: Обновление данных в уже запущенном приложении

Если вы обновили данные (Шаг 1), пока приложение было запущено, оно **не обновит их автоматически** из-за системы кэширования.

Чтобы увидеть новые данные, сделайте одно из двух:
*   **Простой способ:** Остановите и перезапустите сервер `streamlit run app.py`.
*   **Быстрый способ:** В самом приложении, в правом верхнем углу, нажмите на меню (три полоски) и выберите **"Clear cache"** ("Очистить кэш").

---

## 📂 Файловая структура

*   **`app.py`**: Основной файл интерактивного веб-приложения.
*   **`src/scrape_polovni_botasaurus.py`**: Скрипт для сбора данных с `polovniautomobili.com`.
*   **`src/scrape_mobile_de.py`**: Скрипт для сбора данных с `mobile.de`.
*   **`data/raw/`**: Директория для хранения "сырых" данных (`polovni_automobili.csv`, `mobile_de.csv`).
*   **`results/`**: Директория для сохранения HTML-отчетов.
*   **`requirements.txt`**: Список всех необходимых библиотек.
*   **`_debug/`**: Директория для хранения отладочных файлов, которые могут пригодиться при изменении структуры сайтов.

---

## 💡 Заметки для разработчиков

В ходе работы над кодом были выявлены и решены следующие проблемы, которые могут возникнуть снова при дальнейшей разработке:

1.  **Проблемы с f-строками и HTML/CSS:**
    *   **Проблема:** При генерации HTML-отчета возникала ошибка `NameError: name 'font' is not defined`. Это происходило из-за того, что CSS-свойство `font-family` внутри f-строки интерпретировалось как переменная Python.
    *   **Решение:** Весь блок CSS был вынесен в отдельную переменную, которая затем подставлялась в основной HTML-шаблон. Это позволило избежать конфликтов парсера f-строк с синтаксисом CSS.
    *   **Рекомендация:** При работе с большими HTML-шаблонами, содержащими CSS или JavaScript, рекомендуется выносить их в отдельные переменные или даже файлы, чтобы не сталкиваться с проблемами экранирования.

2.  **Динамические ID в Pandas Styler:**
    *   **Проблема:** Попытка стилизовать таблицу статистики в HTML-отчете с помощью статического CSS-селектора (`#stats-table`) не давала результата, так как `pandas.Styler` генерировал для таблицы динамический ID (например, `id="T_6a16f"`).
    *   **Решение:** Был использован параметр `table_uuid` в методе `.to_html()`. Это позволило задать таблице предсказуемый идентификатор (`T_stats-table`), к которому уже можно было применить стили.
    *   **Рекомендация:** При стилизации таблиц, сгенерированных через `pandas.Styler`, всегда используйте `table_uuid`, чтобы иметь возможность надежно обращаться к ним из CSS.

3.  **Отсутствие элементов в HTML-отчете:**
    *   **Проблема:** Блок с информацией о количестве объявлений отображался в Streamlit-приложении, но отсутствовал в сохраненном HTML-файле.
    *   **Решение:** Логика генерации этого блока была продублирована для HTML-отчета. `st.write` был заменен на формирование соответствующего HTML-тега (`<p>`).
    *   **Рекомендация:** Всегда помните, что логика отображения в Streamlit (`st.*`) и логика генерации HTML-отчета — это два разных потока. Любые элементы, которые должны присутствовать в отчете, должны быть явно добавлены в строку `report_html`.

---

## 📊 Пояснения по статистике и анализу цен

Когда вы сравниваете цены на одну и ту же модель автомобиля с разных сайтов, важно понимать, что означают различные статистические показатели и как их интерпретировать.

### 1. Сводная статистика цен

Когда вы выбираете модель для сравнения, вы видите таблицу со статистикой по каждому источнику (`polovni_automobili` и `mobile.de`).

*   **`mean` (Среднее)**: Это обычная средняя цена. Сумма всех цен, деленная на количество объявлений.
*   **`median` (Медиана)**: Это цена, которая находится ровно посередине, если отсортировать все цены по возрастанию. Половина объявлений будет дешевле медианы, половина — дороже. **Медиана лучше подходит для сравнения цен**, так как она менее чувствительна к очень высоким или очень низким ценам (выбросам), которые могут исказить среднее значение.
*   **`std` (Стандартное отклонение)**: Это показатель разброса цен. Чем больше стандартное отклонение, тем сильнее цены отличаются друг от друга. Большое `std` может указывать на то, что на сайте есть как очень дешевые, так и очень дорогие предложения для данной модели.
*   **`25th_percentile` (25-й процентиль)**: 25% объявлений имеют цену ниже этого значения.
*   **`75th_percentile` (75-й процентиль)**: 75% объявлений имеют цену ниже этого значения (или 25% — выше).
    *   **Диапазон между 25-м и 75-м процентилями (межквартильный размах)** содержит 50% всех объявлений и показывает "основной" ценовой диапазон.

### 2. Процентная разница медианных цен

Это ключевой показатель, который я добавил. Он показывает, насколько медианная цена на одном сайте отличается от медианной цены на другом, выраженная в процентах.

*   **Положительное значение (например, `+10.50%`)**: Означает, что медианная цена на втором сайте (например, `mobile.de`) **выше** на указанный процент по сравнению с первым сайтом (например, `polovni_automobili`).
*   **Отрицательное значение (например, `-5.20%`)**: Означает, что медианная цена на втором сайте **ниже** на указанный процент по сравнению с первым.
*   **Нулевое значение (`0.00%`)**: Медианные цены на обоих сайтах одинаковы.

**Как это использовать:** Если вы видите, что `mobile.de` на 15% дороже `polovniautomobili` для одной и той же модели, это может указывать на то, что на `mobile.de` в среднем представлены более дорогие комплектации, или рынок там просто дороже.

### 3. Box Plot (Ящик с усами)

Этот график — ваш лучший друг для визуального сравнения распределений цен.

*   **Центральная линия внутри "ящика"**: Это **медиана** (та же, что и в таблице).
*   **"Ящик"**: Охватывает диапазон от 25-го до 75-го процентиля (межквартильный размах). Внутри этого ящика находится 50% всех объявлений. Ширина ящика показывает, насколько "сжаты" или "растянуты" цены в основной массе.
*   **"Усы"**: Протягиваются от ящика до минимального и максимального значений, которые не считаются выбросами. Они показывают полный диапазон цен без учета экстремальных выбросов.
*   **Точки за пределами "усов"**: Это **выбросы** (outliers) — объявления с ценами, которые значительно отличаются от основной массы.

**Как интерпретировать Box Plot:**

*   **Положение ящиков**: Если ящик одного сайта расположен значительно выше или ниже ящика другого, это подтверждает существенную разницу в медианных ценах.
*   **Размер ящиков**: Если ящик одного сайта шире, это означает больший разброс цен (большее стандартное отклонение) на этом сайте.
*   **Длина усов**: Показывает полный диапазон цен.
*   **Выбросы**: Наличие множества точек-выбросов может указывать на очень дорогие или очень дешевые уникальные предложения, которые могут искажать среднюю цену, но не влияют на медиану.

**Общий вывод:**

Используя эти метрики и график, вы можете получить гораздо более полное представление о ценовой ситуации для конкретной модели на разных рынках. Если медианы сильно отличаются, а ящики не перекрываются, это явный признак того, что рынки существенно разные. Если медианы близки, но ящики и усы сильно отличаются, это говорит о разном распределении предложений (например, на одном сайте больше дешевых или дорогих вариантов).