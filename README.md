# Интерактивный анализатор рынка подержанных автомобилей

Этот проект представляет собой комплексный инструмент для анализа рынка подержанных автомобилей с нескольких сайтов, включая `polovniautomobili.com` и `mobile.de`.

Он состоит из двух основных частей:
1.  **Скрипты для сбора данных:** Гибкие скраперы для каждого сайта (`src/scrape_polovni_botasaurus.py`, `src/scrape_mobile_de.py`), которые собирают объявления по заданным поисковым запросам.
2.  **Интерактивное веб-приложение (`app.py`):** Приложение на базе Streamlit для визуализации, фильтрации и сравнительного анализа собранных данных.

---

## 🚀 Основные возможности приложения

*   **Сравнение рынков:** Возможность загружать и сравнивать данные по одной и той же модели с разных сайтов.
*   **Сравнительный анализ:** Одновременное отображение нескольких групп автомобилей (например, BMW X5 с `polovniautomobili.com` и BMW X5 с `mobile.de`) на одном графике.
*   **Линии тренда:** Автоматическое построение отдельных линий регрессии для каждой группы, показывающих зависимость цены от пробега и года.
*   **Интерактивность:**
    *   Кликабельные точки на графике, ведущие на страницу объявления (функция работает как в приложении, так и в HTML-отчетах).
    *   Гибкие фильтры по источнику данных, группам, году выпуска и пробегу.
*   **Поиск выгодных предложений:** Умная таблица, которая показывает Топ-2 самых дешевых автомобиля для каждой группы в рамках выбранной категории пробега (0-50 тыс. км, 50-100 тыс. км и т.д.).
*   **Экспорт отчетов:** Возможность сохранить текущий вид анализа (график + таблица) в единый, интерактивный и сортируемый HTML-файл.

---

## 🛠️ Рабочий процесс

### Шаг 1: Сбор и обновление данных

Вы можете собирать данные с любого из поддерживаемых сайтов, запуская соответствующий скрипт.

#### Сбор данных с `polovniautomobili.com`

1.  **Настройка:** Откройте файл `src/scrape_polovni_botasaurus.py`. В самом низу файла найдите и отредактируйте словарь `SEARCH_QUERIES`, добавив или изменив нужные вам ссылки для поиска.
2.  **Запуск:** Выполните в терминале команду:
    ```bash
    python3 src/scrape_polovni_botasaurus.py
    ```

#### Сбор данных с `mobile.de`

1.  **Настройка:** Откройте файл `src/scrape_mobile_de.py`. В самом низу файла найдите и измените переменную `SEARCH_URL` на нужную вам ссылку для поиска.
2.  **Запуск:** Выполните в терминале команду:
    ```bash
    python3 src/scrape_mobile_de.py
    ```

### Шаг 2: Запуск интерактивного приложения

Для анализа и визуализации данных запустите приложение:

```bash
streamlit run app.py
```
В вашем браузере откроется вкладка с приложением. В боковой панели вы сможете выбрать, данные с каких сайтов и по каким моделям вы хотите видеть.

### Шаг 3: Обновление данных в уже запущенном приложении

Если вы обновили данные (Шаг 1), пока приложение было запущено, оно **не обновит их автоматически** из-за системы кэширования.

Чтобы увидеть новые данные, сделайте одно из двух:
*   **Простой способ:** Остановите и перезапустите сервер `streamlit run app.py`.
*   **Быстрый способ:** В самом приложении, в правом верхнем углу, нажмите на меню (три полоски) и выберите **"Clear cache"** ("Очистить кэш").

---

## 📂 Файловая структура

*   **`app.py`**: Основной файл интерактивного веб-приложения.
*   **`src/scrape_polovni_botasaurus.py`**: Скрипт для сбора данных с `polovniautomobili.com`.
*   **`src/scrape_mobile_de.py`**: Скрипт для сбора данных с `mobile.de`.
*   **`data/raw/`**: Директория для хранения "сырых" данных (`polovni_automobili.csv`, `mobile_de.csv`).
*   **`results/`**: Директория для сохранения HTML-отчетов.
*   **`requirements.txt`**: Список всех необходимых библиотек.
*   **`_debug/`**: Директория для хранения отладочных файлов, которые могут пригодиться при изменении структуры сайтов.

---

## 💡 История доработок и решенные проблемы

В ходе разработки проект прошел несколько стадий улучшений и исправлений:

1.  **Ошибки парсинга:** Изначально скрапер некорректно обрабатывал объявления со скидкой, "склеивая" старую и новую цену. Проблема была решена путем перехода на использование `data-price` атрибута, что гарантировало получение корректной цены.

2.  **Эволюция логики "выгодных предложений":**
    *   Изначально "кандидаты" определялись статистически (по z-score).
    *   Затем логика была изменена на поиск Топ-3 самых дешевых машин в каждом диапазоне пробега (0-50 тыс. км и т.д.).
    *   Финальная, самая продвинутая версия, ищет Топ-2 самых дешевых предложения для **каждой марки** в каждом диапазоне пробега. Это обеспечивает разнообразие и полноту анализа.

3.  **Интерактивность графика:** Стандартный график Streamlit не поддерживал кликабельные точки. Это было решено путем генерации полного HTML-кода для графика и внедрения в него кастомного JavaScript-кода, который отслеживает клики и открывает соответствующее объявление в новой вкладке.

4.  **Сортируемая таблица в отчете:** Сохраненная в HTML-файл таблица изначально была статичной. Была добавлена легковесная JavaScript-библиотека (`vanilla-js-tablesort`), которая делает таблицу в финальном отчете полностью интерактивной и позволяет сортировать ее по любой колонке.

5.  **Проблема с кодировкой на мобильных устройствах:**
    *   **Проблема:** Сохраненный HTML-отчет корректно отображался на компьютере, но на мобильных устройствах кириллический текст превращался в нечитаемые символы.
    *   **Решение:** В заголовок (`<head>`) генерируемого HTML-файла был добавлен мета-тег `<meta charset="UTF-8">`. Этот тег явно указывает браузеру использовать правильную кодировку (UTF-8), что гарантирует корректное отображение символов на любых устройствах.

6.  **Сложный парсинг сайта `mobile.de` (подробное руководство для ИИ-моделей):**
    *   **Проблема и первоначальные трудности:**
        *   **Блокировка запросов (403 Forbidden):** Первые попытки сбора данных с `mobile.de` с использованием прямых HTTP-запросов (`botasaurus_requests`) приводили к ошибке `403 Forbidden`. Это стандартная анти-бот мера, которая блокирует запросы без корректных заголовков браузера.
        *   **Неэффективность HTML-парсинга:** После перехода на эмуляцию полноценного браузера (`botasaurus.browser`), стандартные CSS-селекторы (`.cBox-body--resultitem`, `.headline-block .h3` и т.д.) не находили ожидаемых элементов. Это указывало на то, что основной контент страницы генерируется клиентской стороной с помощью JavaScript и не присутствует в исходном HTML-ответе сервера.
    *   **Детальное расследование (ключевой момент):**
        *   Для понимания реальной структуры страницы был использован метод сохранения полного HTML-кода, который видит браузер (`driver.page_html`), в отладочный файл (`_debug/mobile_de_debug.html`).
        *   Анализ этого файла показал, что данные об автомобилях не встроены напрямую в HTML-разметку, а передаются в виде большого JSON-объекта, инкапсулированного в JavaScript-переменную `window.__INITIAL_STATE__` внутри тега `<script>`. Это распространенный паттерн для сайтов, использующих SSR (Server-Side Rendering) или CSR (Client-Side Rendering) фреймворки (например, React, Next.js).
    *   **Разработка надежного решения (двухэтапный парсинг JSON):**
        *   **Этап 1: Извлечение сырой JSON-строки из `<script>` тега.**
            *   Использовался `BeautifulSoup` для поиска тега `<script>`, содержащего `window.__INITIAL_STATE__`. 
            *   Затем применялось регулярное выражение `r"window\.__INITIAL_STATE__\s*=\s*(.*)"` с флагом `re.DOTALL` для захвата всего содержимого после знака равенства. Это "жадное" выражение гарантирует, что весь JSON-объект будет захвачен, даже если он разбит на несколько строк.
        *   **Этап 2: "Умная" балансировка скобок для выделения чистого JSON.**
            *   Поскольку "жадное" регулярное выражение могло захватить и другой JavaScript-код, следующий за JSON-объектом (что приводило к `json.decoder.JSONDecodeError: Extra data`), была реализована специальная функция `find_and_clean_json`.
            *   Эта функция итерируется по извлеченной строке, подсчитывая открывающие (`{`) и закрывающие (`}`) фигурные скобки. Как только счетник возвращается к нулю, это означает, что найден конец первого полноценного JSON-объекта. Все символы после этой точки отбрасываются.
            *   Это решение является крайне устойчивым, так как оно не зависит от конкретных символов-разделителей (например, `;`) или форматирования, а полагается на фундаментальную структуру JSON.
        *   **Навигация по JSON-структуре:** После успешного парсинга JSON-объекта, данные об автомобилях были найдены по пути `data['search']['srp']['data']['searchResults']['items']`.
    *   **Выводы и рекомендации для ИИ-моделей:**
        *   При скрапинге современных веб-сайтов, если стандартные CSS-селекторы не работают, всегда проверяйте наличие встроенных JSON-объектов в тегах `<script>`, особенно тех, что содержат `window.__INITIAL_STATE__` или аналогичные переменные.
        *   Будьте готовы к тому, что извлечение JSON может потребовать нетривиальных регулярных выражений и/или логики балансировки скобок для корректного выделения объекта.
        *   Всегда сохраняйте и анализируйте *отрендеренный* HTML-код (полученный через браузер), чтобы точно понимать, какую структуру видит ваш скрапер.

7.  **Восстановление интерактивности графика:** В процессе обновления `app.py` для поддержки нескольких источников данных, была случайно упущена вставка JavaScript-кода в основной график приложения (она осталась только для HTML-отчетов). Ошибка была найдена, и код, отвечающий за кликабельность точек на графике, был восстановлен для основного интерфейса.

8.  **Оптимизация файловой структуры:**
    *   **Проблема:** В процессе разработки и отладки накопились временные и устаревшие файлы, которые засоряли репозиторий.
    *   **Решение:** Были удалены устаревший скрипт `src/analyze_price_vs_mileage.py` и пустые директории `notebooks/` и `tests/`. Временные отладочные файлы (`polovni_automobili.html`, `error_logs/`, `output/`) также были удалены. Наиболее ценные отладочные артефакты (`mobile_de_debug.html`, `mobile_de_state.json`) были перемещены в новую, специально созданную директорию `_debug/` и добавлены в `.gitignore` для удобства управления версиями.
